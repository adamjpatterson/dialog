{
  "name": "test_twilio",
  "version": "1.0.0",
  "main": "./dist/main.js",
  "scripts": {
    "clean": "rm -rf ./dist/*",
    "build": "tsc --build .",
    "clean:build": "npm run clean && npm run build",
    "lint": "npx eslint .",
    "format": "npx prettier --write .",
    "format:lint": "npm run format && npm run lint",
    "test": "npm run clean:build && node --env-file=../../.env --expose-gc --trace-warnings .",
    "monitor": "nodemon ."
  },
  "engines": {
    "npm": ">=10.8.2",
    "node": ">=20.19.0"
  },
  "author": "",
  "license": "MIT",
  "description": "Dialog Twilio integration test.",
  "devDependencies": {
    "@eslint/js": "^9.37.0",
    "@stylistic/eslint-plugin": "^5.10.0",
    "@types/node": "^22.13.14",
    "eslint": "^9.39.5",
    "globals": "^16.5.0",
    "nodemon": "^3.1.14",
    "prettier": "^3.9.6",
    "typescript": "^5.9.3",
    "typescript-eslint": "^8.67.0"
  },
  "type": "module",
  "dependencies": {
    "@far-analytics/dialog": "file:../.."
  },
  "private": true
}
